name: Deploy Mobile Web

on:
  push:
    branches: [main]
    paths:
      - "apps/mobile-web/**"
  workflow_dispatch:

concurrency:
  group: deploy-mobile-web
  cancel-in-progress: true

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.WEB_SSH_HOST }}
          username: ${{ secrets.WEB_SSH_USER }}
          key: ${{ secrets.WEB_SSH_KEY }}
          port: ${{ secrets.WEB_SSH_PORT || 22 }}
          script: |
            set -e
            export PATH="/home/admin/.bun/bin:/home/admin/.nvm/versions/node/v24.16.0/bin:$PATH"
            cd ${{ secrets.MOBILE_WEB_DEPLOY_PATH }}
            git pull origin main
            cd apps/mobile-web
            bun install
            bun run build
            pm2 restart mobile-aurict-web
            pm2 save
